pipeline {
  agent {
  dockerfile true
  }
   options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Example') {
       steps {
          echo 'hello world!'
          sh '''
            docker ps
          '''
         }
       }
     }
   }
